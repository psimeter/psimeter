# dependencies / build output
node_modules/
dist/
**/target/
coverage/

# python
.venv/
__pycache__/
*.pyc

# local dev ledger output (regenerated; not the published dataset).
# Ignore EVERYTHING under ledger/ — *.jsonl, blobs/, anchors/, witness feeds,
# the witness key (a private key!), .tsr/.ots proofs, contacts — except the
# tracked .gitkeep placeholder. No dev-run artifact is ever committable.
ledger/*
!ledger/.gitkeep

# misc
*.log
.DS_Store

# local agent / editor config (not part of the published project)
.claude/
