{
  "name": "@psymeter/witness",
  "version": "0.0.0",
  "private": true,
  "description": "PsyMeter independent live-witness node: co-signs checkpoints/choices in real time, binding a fresh self-verified drand round + RFC 3161 TSA, on its own append-only feed (spec D16).",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@noble/curves": "^1.6.0",
    "@psymeter/core": "*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
